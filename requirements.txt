# ============================================================
# TyAgent / GeoAgent V1.0 - Python 依赖清单
# 安装方式（务必使用 uv，禁用裸 pip；走清华镜像加速）：
#   uv pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# ============================================================

# --- Web 框架与 ASGI ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.3

# --- 配置与数据校验 ---
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1

# --- 数据库（SQLAlchemy 2.0 async + asyncpg；PostgreSQL 仅存会话/消息） ---
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.29.0

# --- 向量库（Milvus，3.5 阶段启用） ---
pymilvus>=2.6.0

# --- 知识图谱（Neo4j，3.6 阶段启用） ---
neo4j>=5.20.0

# --- 测试 ---
httpx>=0.27.0
pytest>=8.3.0
pytest-asyncio>=0.24.0

# --- 代码质量 ---
ruff>=0.6.9

# --- LLM 网关（3.2 阶段启用） ---
litellm>=1.50.0

# --- Agent 编排（3.3 阶段启用） ---
langgraph>=0.2.30
langchain-core>=0.3.0
langchain-openai>=0.2.0
